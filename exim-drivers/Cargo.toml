[package]
name = "exim-drivers"
version = "0.1.0"
edition = "2021"

[dependencies]
exim-store = { path = "../exim-store" }
inventory = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
